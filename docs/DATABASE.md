# Database

Modèle de données de POSTYNC.

La base de données n'est pas encore créée. Les migrations et les jeux de données
de démarrage seront placés dans `supabase/migrations/` et `supabase/seed/`.

Ce document décrira les tables, les relations et les politiques d'accès une fois
la base définie.
